const car = {
    make: 'Honda',
    model: 'Accord',
    year: 2020
}

// make, model and year are the key to the object
const keys = Object.keys(car)
console.log(keys);

// output is ['make', 'model', 'year']