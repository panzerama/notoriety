const food = {
    types: 'only pizza'
}

// these 2 lines of code print out exactly same thing
console.log(food['types']);  // output is "only pizza"
//console.log(food.types);     // output is "only pizza" 
